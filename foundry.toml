[profile.default]
src = "src"
out = "out"
libs = ["lib"]
remappings = ["@chainlink/contracts/=lib/chainlink-brownie-contracts/contracts/"]
fs_permissions = [{ access = "read", path = "./broadcast" }]



[profile.snapshot]
# this is where forge snapshot will write the gas report
snapshots = "gas-snapshot"